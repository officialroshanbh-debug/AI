{
  "buildCommand": "prisma generate && (prisma migrate deploy || prisma db push --accept-data-loss --skip-generate) && next build",
  "framework": "nextjs",
  "regions": [
    "sin1",
    "iad1"
  ],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 300
    }
  }
}