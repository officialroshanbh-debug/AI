{
  "buildCommand": "prisma generate && (prisma migrate deploy || prisma db push --skip-generate) && next build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 300
    }
  }
}
