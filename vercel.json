{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss --skip-generate && next build",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 300
    }
  }
}
